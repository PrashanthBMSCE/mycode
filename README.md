# mycod  sdk.Lists.registerThreadRowViewHandler(function(ThreadRowView){
                ThreadRowView.addImage({
                    imageUrl:imgURL,
                    imageClass:'abc',
                    tooltip:'reddy'

                })
            })
            
